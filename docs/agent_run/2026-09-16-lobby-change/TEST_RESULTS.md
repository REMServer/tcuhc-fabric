# 验证结果：NBT 大厅、侧边栏、奖励箱

日期：2026-09-16。以下为本次开发中实际执行的本地检查；GitHub CI 结果应以 PR 中对应提交的检查为准。

| 检查 | 结果 | 覆盖 |
| --- | --- | --- |
| `python3 tests/run_all.py` | 6 组通过 | 仓库约束、Mixin 登记、已有玩法与预生成回归 |
| `bash gradlew --offline --no-daemon --console=plain clean build`，Java 21 | 通过 | 正式编译、打包及 remap |
| Mixin 登记审计 | 48/48 | 两个新增大厅 Mixin 已登记 |
| 独立 Fabric 专用服大厅检查 | 18 项通过 | 安全落点、世界数据读取、保护边界、清理与装饰去重 |
| 独立 Fabric 专用服侧边栏检查 | 104 项通过 | 时间、人数、队伍、边界、颜色与新局清理 |
| 独立 Fabric 专用服附魔书检查 | 24 项通过 | 附魔池、抽取概率、合法等级及实际填箱 |
| Minecraft 原生 NBT 读写往返 | 通过 | 装修后模板方块、方块实体、实体数据一致 |
| 正式 JAR 内容检查 | 通过 | 只有生产入口，无临时测试类，Iceland 模板与源码资源一致 |
| 开发服正常启动及局域网状态握手 | 通过 | `Done`、RCON、Minecraft 1.21.1 / protocol 767 |

## 专项检查细节

大厅验证覆盖落点地板及头部净空、元数据读取不写回、开放岛底、大厅内外及跨维度保护范围、非创造破坏/交互限制、掉落检测、计划刻边界、完整清理与重复重建去重。正常重启后确认仍为 6 个装饰盔甲架，模板底部高度保持 Y=160。

侧边栏从早期 40 项扩展到 74 项，最终为 104 项；这些是同一套检查的迭代结果，不相加。覆盖 0、1、59、60、61、3600、5400 秒、归零与超时、旁观者/空队排除、离线参赛者、死亡及恢复、实际洞穴任务产生的边界信息、新局清理，以及文字相同但旧颜色为白色时的修正。

附魔书穷举每种战斗类型的 64 个抽取结果，每类型随机生成 4000 本书检查池和等级，并通过实际填箱入口验证 256 个种子。另检查单个空槽位、满箱不覆盖、空箱不出书、与珍贵物品共存，以及水下速掘 II 限制为 I。

## 证据位置与复现范围

本机保留的证据位于忽略目录：

- `run/iceland-smoke/runtime.log`：大厅专用服检查。
- `run/sidebar-smoke/runtime.log`：`SIDEBAR_SMOKE_PASSED checks=104` 和 `BOOK_SMOKE_PASSED checks=24`。
- `run/books-build.log`：正常 `clean build` 成功。
- `run/lobby-exports/20260916-152830/`：装修后的模板导出、备份及报告。

这些临时测试入口及日志不在本次提交中，因此上述专项检查不是现有 CI 自动覆盖项。维护者可以运行仓库已有测试和构建；检查真实游戏行为时，请使用独立开发世界逐项验证大厅加入/掉落返回、重启后装饰数量、开局清理、赛后重建、侧边栏显示和新生成的奖励箱。

## 验收边界

- 专项检查直接调用真实专用服中的生命周期及填箱方法，部分参赛数据为模拟记录，不能视为已完成真实多客户端整局 UHC 验收。
- 服务端文本样式已验证；最终颜色、布局和长文本在不同客户端 GUI 缩放下仍需视觉验收。
- 新奖励规则不会改写已预生成世界里的旧箱子，不能用检查旧箱子的结果判断新池是否生效。
- 本次没有逐个验证其他结构箱的战利品路径，也未修改原版附魔台或村民附魔书交易。
